% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_pca.R
\name{compute_pca}
\alias{compute_pca}
\title{Compute PCA from a genind object}
\usage{
compute_pca(genind, popmap = NULL, nf = 10)
}
\arguments{
\item{genind}{A \code{genind} object, typically from \code{vcfR2genind()} after
SNP filtering.}

\item{popmap}{Optional data frame with columns \code{id} and \code{pop}. If
supplied, populations are matched to \code{indNames(genind)} and assigned
to the genind before PCA.}

\item{nf}{Number of axes to retain. Default 10.}
}
\value{
A list with components:
\describe{
\item{scores}{Data frame of sample scores with \code{id}, \code{pop}, and
\code{PC1}...\code{PCn} columns.}
\item{var_pct}{Numeric vector of percent variance explained per axis.}
\item{loadings}{Variable loadings (data frame).}
\item{pca}{The raw \code{dudi.pca} result object.}
}
}
\description{
Replicates the PCA logic used by \code{SNPfiltR::assess_missing_data_pca()}:
allele frequencies are extracted with NA imputed by column mean, then
\code{ade4::dudi.pca()} is run unscaled.
}
\examples{
\dontrun{
gen <- readRDS("GENIND_filtered.rds")
popmap <- read.table("popmap.txt", header = TRUE)
res <- compute_pca(gen, popmap)
head(res$scores)
}

}
